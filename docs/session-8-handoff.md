# Session 8 handoff

This checkpoint is source-only. No C++/Emscripten compilation, TypeScript checking, dependency
installation, Vite bundling, preview, server, browser runtime, or project execution was invoked.
The only executable project checks were source-text contract and delimiter scanners; archive/PDF
inspection and lexical source scans do not execute the application.

## Reference provenance

Only the user-supplied reference set was used. No alternate RDP implementation or reference-code
site was consulted.

| Attachment | SHA-256 |
| --- | --- |
| `dnaDLLSource(3).zip` | `8a79a2adf7e733c57fb91cb0c50c5fd0e1183d528e7db45175efbcddc524b165` |
| `dna5DLLSource(3).zip` | `1fb01d49ed4513de9bd262aa943f82b16fcac3396cb449f2deb7751a10bf9102` |
| `VB Source(3).zip` | `6b1400378ac15c276a2030807c922e1826ddea557606e4fb718812acdf3e6933` |
| `RDP5Manual(3).pdf` | `ce9b4b0c88f3a93076e5d90502f855e247b0df6cc1bec82401e82dbfd3dc416c` |

The manual's load → inspect/mask → preliminary scan → hypothesis refinement → ordered review and
repair → save/export workflow remains the product-level contract. The new late stages feed the same
automatic two-of-three group used by cyclic tract erasure; they do not bypass ordered review.

## Completed in this phase

1. Traced both active native `FinalTrim` call shapes through the RFF guard, selected-role cleanup,
   and per-method signal recheck. The line-level mapping is in
   `docs/native-late-consensus-trace.md`.
2. Completed the active RFF=0 list build after nearest-nonrecombinant `OKSeq 6`: correlation-gated
   first expansion, raw-tree-bounded second expansion, and source-ordered admission.
3. Ported selected-role pruning across all three mapped lists, including swap-last removal, exact
   strict/equality operators, the asymmetric matrix index spelling, and the inherited third-list
   loop index after the second list shrinks.
4. Activated final `OKSeq 15` membership and completed CScore in source order: membership multiplier,
   `RCorrX`, and the declared-`Long` `NS` accumulation with half-to-even narrowing after every native
   assignment.
5. Ported all three `ConsensusOK` reconstruction stages: primary thresholds/topology checks,
   six-distance equivalence or low-score direct widening, raw/direct straggler collection, and
   restoration of all three pre-rebuild lists if any role is empty.
6. Ported the shared selected-role block after the RFF guard: Single-precision movement sums,
   representative ranks, raw/direct outlier and clade constraints, cross-region and sixfold gates,
   swap-last deletion, bounded admission, and the unconditional strict-inlier branch implied by the
   supplied `Or x = x` expression.
7. Retained two source quirks that are easy to accidentally “fix”: the mixed
   `FAMatSmall <= HDF` comparison and the literal full-matrix sequence-zero fallback for role zero.
8. Ported the primary-RDP post-group recheck. Each finalized nonrepresentative list candidate is
   rerun against that role's two working representatives with the supplied `LowP * 100000` threshold
   lift. Emitted signals, candidate-recombinant signals, event-overlapping traces, ordinary corrected
   significance, and the best tract/probabilities are separate evidence fields.
9. Kept the global cyclic scan catalogue authoritative while exposing the widened-threshold recheck
   compactly per event. This avoids duplicating weak recheck records into later event discovery while
   preserving the native decision evidence needed for review and parity work.
10. Fed the finalized distance membership into every role's two-of-three evidence set and the
    automatic co-recombinant group. Manual group overrides remain distinct and continue to drive
    accepted-event erasure and final alignment exports.
11. Added symmetric direct/raw/collapsed distance caches for the quadratic widening/cleanup passes.
    Primary-RDP rechecks and masked traces reuse one capacity-retaining information-rich profile
    buffer rather than allocating an alignment-sized profile per candidate.
12. Expanded project/result JSON, event CSV, static TypeScript contracts, and the review matrix with
    expansion/pruning/rebuild/fallback state, final membership, and post-group RDP recheck evidence.
13. Implemented the manual's RDP-specific uncertainty rule through the supplied `CheckEndsVB`/DLL
    path: current information positions after prior erasures, source-shaped ten-character input
    `MissingData`, distinct beginning/ending ranges, strict linear edge gates, and the literal wrap
    comparison. Immediate contact, qualifying erased events, nearest information-rich count, native
    check range, and reason flags are separate across event/alignment JSON, CSV, and review UI. The
    line-level mapping is in `docs/native-breakpoint-uncertainty-trace.md`.
14. Advanced the engine/source checkpoint to `0.8.0-session-8` and project schema to `v1alpha8`,
    retaining import support for `v1alpha1` through `v1alpha8`.
15. Refreshed the Session 7 GitHub Pages workflow to the current official Node-24-generation action
    releases and enabled hidden-file inclusion so the verified `.nojekyll` marker reaches the
    artifact. With repository contents at the root and **Settings → Pages → Source: GitHub Actions**,
    a default-branch push or manual dispatch builds and publishes the verified static artifact; no
    hosting service or deployment branch is required. The project build remains Node 20 plus pinned
    Emscripten 5.0.1, provisioned by the current `setup-emsdk@v16` wrapper.
16. Traced the complete supplied statistical breakpoint path before implementation: active
    `PolishBP(20)` invocation, three-state information alphabet, DNA5 serial Viterbi training,
    forward/reverse posterior normalization, strict 95%/99% source thresholds, signed nearest-
    interval matching, missing-data/segment repositioning, and final three-usable-site revert. The
    line-level implementation map is in `docs/native-breakpoint-confidence-trace.md`.
17. Ported that active non-segmented BURT/`PolishBP` unit in `burt_confidence.cpp`: three-symbol
    recoding, source-shaped circular expansion and sentinel, 21 seeded Viterbi starts with exact-
    likelihood convergence and pseudocount updates, forward/reverse posterior normalization, strict
    `0.995`/`0.999` interval searches, signed nearest matching, endpoint assignment, input
    `MissingData` repositioning, three-usable-site revert, and final active gap-search quirks. A local
    15-bit Microsoft-CRT random adapter follows the supplied Windows DLL's `srand(3)`/`rand()` stream.
18. Applied available polished coordinates after representative refinement and before cyclic
    erasure; representative-only corrections rerun and apply the path, explicit manual breakpoint
    edits remain authoritative, and restored projects recompute evidence without rewriting saved
    coordinates. JSON/project/CSV/alignment payloads and the review UI retain both signed statistical
    intervals and the separate parent-state/CheckEnds diagnostics.
19. Restored the supplied default-enabled breakpoint-polishing control as a modern settings toggle
    and carried it through the C ABI, worker, result/project JSON, and replay. Disabled analyses skip
    all BenHMM work and explicitly preserve the detected or manually entered coordinates. The
    alignment-sized lattice/backpointer/path buffers now follow the DLL's single `calloc` lifetime
    per event rather than being cleared on every Viterbi iteration.
20. Corrected the tract helper to active `ModSeqNumY` parity: both breakpoint sites are erased in
    linear and wrapped tracts, with the inclusive definition shared by fragment re-entry, erased-
    tract context, event-overlap grouping, and final FASTA exports. Equal endpoints follow the
    source's wrapped/full-alignment branch and are labelled as wrapping for circular results.
21. Added a capacity-reusing `O(L)` BURT missing mask that unions source-shaped input missing runs
    with all prior inclusive erasures affecting the current representatives. This matches the
    supplied `PolishBP`-before-current-`ModSeqNumY` ordering without recreating the native `N x L`
    array per event.
22. Completed the four most common manual section 6 accepted-event FASTA variants. The export page
    now offers current co-group sequence removal, union-tract column removal, per-sequence tract
    masking, and aligned mosaic fragments. All routes use the same core readiness gate, and the new
    routes reject all-sequence/all-column empty outputs explicitly; column removal is `O(E + NL)`.
23. Removed per-candidate temporary-vector churn from the primary triplet loop. Candidate passes now
    append directly into a capacity-retaining scanner buffer while leaving recheck result ownership
    independent; thresholds, discovery order, and retained signal fields are unchanged.
24. Separated manual section 4.1.1 enabled, masked, and disabled sequence states through the React
    curation control, worker/C ABI, core, results, and project replay. Masked rows skip primary
    triplets but participate in secondary RDP and late grouping; disabled rows skip event evidence
    and cannot be assigned roles/groups, while both remain available in bounded event trees.
25. Added the manual's complementary enabled-only and masked-or-disabled-only FASTA saves. These
    preserve untouched aligned rows, remain independent of event-review readiness, and return an
    explicit error rather than a zero-byte download when their selected partition is empty.
26. Restored the supplied `FastRecCheckP` `EN != BE` primary-call gate. A full circular candidate
    run can no longer emit a whole-alignment detection, while equal endpoints remain supported for
    manual/restored events because the later active `ModSeqNumY` path gives them full-span meaning.
27. Restored the manual's bulk sequence-curation workflow with modern auto-mask, enable-all,
    mask-all, and disable-all controls. Bulk changes update primary triplet counts, clear the other
    mutually exclusive state, and remain available for datasets whose table is rendering-capped at
    500 rows.
28. Moved curation FASTA generation onto the loaded immutable alignment plus masks supplied with the
    request. Full, enabled-only, and masked-or-disabled-only FASTA can now be downloaded before a
    scanner exists as well as from the final export page; only event-derived variants retain the
    ordered-review readiness gate.
29. Confirmed the manual's disabled-tree contract against the supplied active source paths: primary
    and secondary scan loops use `MaskSeq < 2`, while general UPGMA construction and tree drawing
    iterate all `NextNo` leaves and colour state 2 separately. The port therefore prevents disabled
    evidence/roles but leaves those rows in the bounded topology itself.

## Browser-scope fidelity boundary

The finalized primary-RDP grouping, post-group RDP recheck, CheckEnds, and BURT/BenHMM paths are now
active in source, but are not compiled or native-golden validated. The desktop block also rechecks
every other enabled method
family on the selected list; those calls remain absent because those detection methods are not yet
ported. The desktop's disk-only excluded-sequence substitution has no static-browser counterpart:
all sequences loaded into the browser remain in memory, and masked trace evidence is already
reported separately.

The event tree panel remains capped at 100 sequences with a labelled cached direct-distance fallback,
fragment retention at 256 records, and `CalcMatchY` at its supplied bounded variable-site workspace.
These limits, exact floating-point equality branches, Single/Long conversion points, and list order
need focused native fixtures before parity can be claimed.

## Static inspection performed

- Rechecked attached archive paths and provenance hashes and inspected the manual's relevant workflow
  pages without executing project code.
- Ran `scripts/check-source-contract.mjs`, which statically aligns all 36 public ABI functions,
  CMake exports, worker calls, package/native version, `v1alpha8` schema, late-consensus status, and
  primary-RDP recheck, erased-tract uncertainty, BURT evidence, sequence curation, six FASTA routes,
  scan scratch reuse, and polish-setting fields.
- Scanned C++/header/TypeScript/TSX delimiters and reviewed JSON emission/type field correspondence.
- Reviewed the Pages workflow and relative-path deployment contract. It is configured but still
  unvalidated until the user elects to run Actions.

## Recommended next fidelity phase

1. Establish focused native `PolishBP`/BURT fixtures for the seeded Windows random stream, odd/even
   circular expansion, signed wrapped ranges, missing-data relocation, role-only recomputation, and
   revert/final-gap edge cases while keeping CheckEnds evidence distinct.
2. Add the remaining detection/role method families and only then enable their native post-group
   rechecks; keep per-method scores and contributions explicit.
3. When execution is authorized, run the smallest resource-limited build and golden fixtures first,
   concentrating on `FinalTrim` list order, Single/Long conversions, exact equality, widened
   post-group RDP thresholds, and circular breakpoints.
