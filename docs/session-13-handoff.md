# Session 13 handoff

## Checkpoint boundary

Session 13 advances the source checkpoint to `0.13.0-session-13` and project schema
`org.rdp-web.project/v1alpha13`. It adds the supplied ordinary automated triplet GENECONV path as a
fourth primary discovery stream beside RDP, MaxChi, and CHIMAERA. Any enabled method can author the
strongest corrected signal entering the existing cyclic event, three-set reconciliation, ordered
review/repair, and export workflow.

The implementation was derived only from the supplied RDP5 manual, VB source, DNA5 DLL source, and
companion DNA DLL source. Its active trace follows `FindSubSeqGCAP6`, `GetFragsP`,
`GetMaxFragScoreP`, `CalcKMaxP`, `GCCalcPValP2`, `GCGetHiPValP`, `DelPValsP`,
`MakeDeleteArrayP`, and `Module31.bas::GCXoverD`. No alternate RDP or GENECONV implementation was
consulted.

Per the project instruction, no supplied/native source, C++, WebAssembly, TypeScript compiler,
Vite build, preview server, dependency install, or project/runtime test was executed. “Implemented”
means source and cross-layer contracts exist; it does not mean compiled or native-parity validated.

## Implemented this session

- A source-shaped kernel with six signed fragment tracks. Three inner pair-match tracks and three outer
  discordant-sequence tracks preserve the supplied provisional recombinant/minor/major mapping.
- Ignored-indel ordinary triplet preparation from the already populated MaxChi equality workspace.
  RDP categories, MaxChi, CHIMAERA, and GENECONV inputs therefore share one alignment-byte pass.
- Supplied mismatch penalties with single-precision narrowing, source Newton initialization for
  lambda/K, a bounded bisection fallback only when Newton becomes numerically unsafe, strict critical
  scores, and the supplied high-score Karlin–Altschul tail branch. Literal low-tail subtraction and
  high-tail underflow are retained; a zero probability remains a literal source value instead of
  being raised to a browser display floor.
- Source-order fragment semantics with the latest-ending tie rule. A single category-run pass
  retains `GetFragsP`'s initial run plus its terminal origin-wrapped duplicate, and coalesces only
  the positive outer runs that the supplied helper coalesces. Prefix scores, next-lower boundaries,
  and a rightmost range-maximum tree then reduce the direct quadratic start-extension loop to
  `O(R log R)` per track while retaining its stop condition.
- Stable lowest-raw-P selection across all six tracks and a lazy range-add/range-maximum coverage
  tree for the supplied overlap rule. This keeps selection `O(F log N)` instead of scanning every
  covered site for every candidate.
- Combined RDP/MaxChi/CHIMAERA/GENECONV strongest-first cyclic scheduling, fragment re-entry,
  event support grouping, shared role reconciliation, BURT polishing, review decisions, and final
  alignment exports. Exact probability ties use the supplied method-major order RDP → GENECONV →
  MaxChi → CHIMAERA even though the browser prepares all enabled kernels in one fused triplet pass.
- Settings for enabling GENECONV, the supplied default mismatch scale `G=1`, and the supplied
  default one-overlap allowance, with bounded validation and complete project persistence.
- Live progress counters for positive starts, above-critical fragments, corrected candidates,
  overlap rejections, and bounded numerical fallbacks.
- Per-signal JSON/restore/CSV evidence: track, polymorphic/positive/discordant site counts, mismatch
  penalty, fragment and critical scores, lambda, K, raw KA probability, project-corrected
  probability, ignored-indel state, overlap filtering, and explicit inactive minimum filters.
- Correction fidelity follows the active automated call rather than its ambiguous presentation:
  raw KA probability is thresholded/stored with `MCCorrection` (the triplet or constrained
  analysis-plan count), while the separate `GCMCCorrection = choose(NS, 2)` desktop display factor
  remains documented for native fixture comparison.
- An on-demand three-colour `-log10(raw KA P)` fragment envelope. Inner/outer track pairs retain the
  supplied colour grouping and use an interval sweep rather than per-fragment range painting.
- A responsive anchor evidence card explaining the six-track role, score, KA parameters,
  probability scopes, active ignored-indel/overlap defaults, and inactive desktop minimum filters.
- Non-coordinate-changing representative and finalized-distance-list corroboration through the same
  ordinary `GCXoverD` six-track kernel. MaxChi prepares the triplet once; CHIMAERA and GENECONV
  reuse its equality/missing-data workspace, while JSON, review, and CSV retain the best stable
  fragment plus workload, skew, overlap, correction, and numerical-fallback evidence.
- ABI and restore coverage: three scan/restore settings, method code `3`,
  `rdp_restore_geneconv_discovery`, five authoritative workload counters, and the Emscripten export.
- Backward restoration: `v1alpha13` restores GENECONV state; `v1alpha12` keeps GENECONV disabled;
  earlier method and analysis-scheme gates remain unchanged.
- Hardened project restoration rejects a hand-edited signal unless its recombinant and two parent
  roles are distinct members of that signal's saved triplet, preventing inconsistent method traces
  from entering event reconstruction.

## Fidelity boundary

GENECONV is **source-shaped active unvalidated**. The ordinary ignored-indel, Karlin–Altschul
triplet route is represented end to end. Authorized native fixtures are still required for
compressed-table ambiguity behavior, circular first/last fragments, equal-P ordering, float
narrowing, Newton edge cases, coordinate aliases, and cross-method event order.
The supplied Module30/Module5 sibling scans' ten-visit repeated-P escape and the zero-score terminal
fragment sentinel are retained as explicit fixture boundaries; the active port follows the traced
Module31 stable-list route until the authorized desktop build identifies the governing sibling.
The overlap tree applies the intended allowance over polymorphic positions; the supplied
`DelPValsP`/`MakeDeleteArrayP` fragment-count-bound indexing quirk is retained as a required
native comparison rather than reproduced as an unsafe array convention.

Permutation probability, manual pair scans, alternative indel-block modes, and full native late
event reconstruction are separate supplied workflows and are not claimed here. The ordinary-kernel
representative/final-list recheck is active but does not stand in for those paths. The three
minimum fragment controls are deliberately reported inactive because their predicates are commented
out in the active automated `GCXoverD` body.

## Static checks allowed for this checkpoint

The source checker covers C ABI/header/CMake/worker names and arities, version/schema gates,
GENECONV source and performance markers, settings/progress/results/types, restore evidence,
review/plot/export surfaces, and old-schema behavior. Delimiter balance, JavaScript syntax, shell
syntax, YAML parsing, stale-version scans, Pages workflow contracts, and archive integrity are also
checked without executing project code.

GitHub Pages remains configured for **Settings → Pages → Source: GitHub Actions**. The workflow
builds the compatibility WASM module and Vite site remotely, validates the source/types/artifact,
uploads `dist` including `.nojekyll`, and deploys only from the repository default branch or a
manual dispatch.

## Known open work

1. Create authorized native ordinary-GENECONV fixtures spanning all six tracks, circular and linear
   ends, ambiguity/missing data, equal scores/P-values, overlap counts, KA edge branches, role maps,
   and combined event order.
2. Trace and port GENECONV permutation, manual pair, alternative-indel, and full late-event paths
   as separately labelled modes.
3. Establish the existing MaxChi, CHIMAERA, BURT/BenHMM, query/reference, and reconciliation golden
   corpora.
4. Continue with the next supplied primary/secondary method family without third-party reference
   code.
5. Run the first tiny resource-limited compile/runtime checkpoint only after explicit permission.

## Resume map

- GENECONV kernel/contracts: `wasm/src/geneconv.hpp`, `wasm/src/geneconv.cpp`
- Fused scheduler/results/plot/CSV: `wasm/src/rdp_method.hpp`, `wasm/src/rdp_method.cpp`
- C ABI/schema/restore: `wasm/include/rdp_api.h`, `wasm/src/rdp_api.cpp`
- Worker scan/restore: `src/workers/analysis.worker.ts`
- Types/defaults: `src/lib/types.ts`, `src/App.tsx`
- Settings/progress/review/plot/export: `src/components/SettingsStep.tsx`,
  `src/components/ScanStep.tsx`, `src/components/ReviewStep.tsx`,
  `src/components/SignalPlot.tsx`, `src/components/ExportStep.tsx`
- Supplied-source trace: `docs/native-geneconv-discovery-trace.md`
- Static contracts: `scripts/check-source-contract.mjs`, `scripts/check-source-balance.mjs`

This checkpoint remains source-only and must be treated as unvalidated until the authorized native
golden/runtime phase.
